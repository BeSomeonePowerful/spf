#include "../satregsm.h"
#include "mocksat.h"

//very simple sm can be ignored
int main()
{
  //build a mock sat,the mock sat will send a con msg on init

  //send a con msg,expecting sm transition
  //recv a conack,expecting sm transition

  return 0;
}
