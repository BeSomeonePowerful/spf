#ifndef  __MSGCONSTDEF_SIP_H
#define  __MSGCONSTDEF_SIP_H

enum SIPMETHODTYPE
{
   SIP_UNKNOWN=0, 
   SIP_ACK, 
   SIP_BYE, 
   SIP_CANCEL, 
   SIP_INVITE, 
   SIP_NOTIFY, 
   SIP_OPTIONS, 
   SIP_REFER, 
   SIP_REGISTER, 
   SIP_SUBSCRIBE, 
   SIP_RESPONSE, 
   SIP_MESSAGE, 
   SIP_INFO, 
   SIP_PRACK, 
   SIP_PUBLISH, 
   SIP_SERVICE, 
   SIP_UPDATE, 
   SIP_MAX_METHODS
}; 

enum SIPTRANSTYPE
{
   TRANS_UNKNOWN=0, 
   TRANS_TLS,
   TRANS_TCP,
   TRANS_UDP,
   TRANS_SCTP,
   TRANS_DCCP,
   TRANS_DTLS,
   TRANS_WS,
   TRANS_WSS,
   TRANS_MAX
};

#endif

